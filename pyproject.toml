[project]
name = "account-health"
version = "0.1.0"
description = "Scores account health from a monthly usage export"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
pythonpath = ["scorer"]
testpaths = ["scorer/tests"]
