{
  "name": "workshop-agentic-sdlc-lab-guides",
  "private": true,
  "type": "module",
  "description": "Authoring-time only. Attendees never run this; it renders guides/*.md.hbs into the two guide formats. The lab guide lives here, not in the front-door repo, because this repository stays sealed until kickoff.",
  "scripts": {
    "build": "node scripts/build-guides.mjs"
  },
  "devDependencies": {
    "handlebars": "^4.7.8"
  }
}
